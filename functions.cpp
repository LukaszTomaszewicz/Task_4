#include "functions.hpp"

void readFile(string path1, string path2)
{
    uint fileSize1{0};
    uint fileSize2{0};
    bool diff { 0 };

    if(fileSize2 != fileSize1)
    {
      // provided files have different size
    }
    else
    {
      // provided files have same size
    }
}
